---
import Quote from "./icons/general/Quote.astro";

const metadata = {
  alt: "Gabe Newell",
  src: "/imgs/gabe.webp",
};
---

<section class="flex flex-col md:flex-row items-center justify-between md:gap-x-18 gap-y-8 w-full mb-12 md:mb-24" aria-label="Cita inspiradora">
  <!-- Img with solid shadow-->
  <figure class="w-64 md:min-w-96 aspect-[3/4] flex relative hover:scale-105 transition-transform duration-500 ease-in-out">
    <img
      src={metadata.src}
      alt={metadata.alt}
      class="rounded-[15px] w-full object-cover object-top relative z-10"
    />
    <!-- Shadow -->
    <div
      class="bg-[#21252B] w-full aspect-[3/4] rounded-[15px] absolute top-4 md:top-15 right-4 md:right-15 z-0 hidden md:block"
      aria-hidden="true"
    >
    </div>
  </figure>
  <!-- Img with solid shadow-->

  <!-- Quote -->
  <div class="gap-y-5 flex flex-col w-full">
    <blockquote class="flex flex-col gap-y-4 md:gap-y-7">
      <Quote class="hidden custom:block size-20 md:size-32" aria-hidden="true" />
      <div class="flex flex-col gap-y-4 md:gap-y-7">
        <p class="text-[#ABB2BF] italic font-light text-2xl md:text-4xl/relaxed">
          La clave del éxito es admitir que no sabes algo y luego descubrir cómo
          aprenderlo
        </p>
        <footer class="text-[#ABB2BF] italic font-light text-2xl md:text-4xl">
          <cite>- Gabe Newell</cite>
        </footer>
      </div>
    </blockquote>
  </div>
  <!-- Quote -->
</section>
