---
interface Props {
  label: string
  name: string
  type?: 'text' | 'email' | 'textarea'
  required?: boolean
  placeholder?: string
  rows?: number
}

const {
  label,
  name,
  type = 'text',
  required = false,
  placeholder = '',
  rows = 4
} = Astro.props
---

<div class="flex flex-col gap-2">
  <label class="text-[#ABB2BF]" for={name}>
    {label} {required && <span class="text-red-500">*</span>}
  </label>
  
  {type === 'textarea' ? (
    <textarea
      id={name}
      name={name}
      required={required}
      placeholder={placeholder}
      rows={rows}
      class="rounded-lg bg-[#21252B] p-2 border border-[#363C49] focus:border-[#569CDE] focus:outline-none text-[#ABB2BF] resize-none"
      aria-describedby={`${name}-error`}
    />
  ) : (
    <input
      type={type}
      id={name}
      name={name}
      required={required}
      placeholder={placeholder}
      class="rounded-lg bg-[#21252B] p-3 border border-[#363C49] focus:border-[#569CDE] focus:outline-none text-[#ABB2BF]"
      aria-describedby={`${name}-error`}
    />
  )}
  <span id={`${name}-error`} class="text-sm text-red-500 min-h-[20px] transition-all duration-200"></span>
</div>
